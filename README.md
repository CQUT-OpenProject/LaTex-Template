## LaTex Template

本仓库提供 `本科生课程考试试卷`、`毕业设计（论文）`、`个人简历` 的 LaTeX 模板，可根据具体信息进行修改。

## Why LaTex?

1. 相较于 Word，LaTex 在排版上有着更强的能力。使用 LaTex 可以让文档看起来更加专业和美观。
2. 相较于 Word，AI 工具更擅长处理 LaTex 文档。

## 安装 LaTex 工具

使用 LaTex 需要安装相应的编辑/编译软件，例如 TeXstudio、MacTeX 等。若不想安装本地工具，也可以使用在线 LaTex 编辑器，例如 Overleaf。

## 许可证

本仓库中的 LaTeX 模板文件（`.cls`、`.tex`、`.latexmkrc` 及相关资源）遵循 [LaTeX Project Public License (LPPL) 1.3c](https://www.latex-project.org/lppl/) 发布。完整许可文本见仓库根目录的 [`LICENSE`](LICENSE)。受 LPPL 约束的范围包括 `个人简历/`、`本科生课程考试试卷/` 与 `毕业设计（论文）/template/` 下的模板源文件及资源（各 `.cls` 文件头部有具体说明）。

`个人简历/fonts/` 目录下的字体为第三方作品，分别遵循其自带的 OFL、Apache License 等许可证，不属于上述 LPPL Work。
